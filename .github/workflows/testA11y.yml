name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "e2e-web-demo" ]
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello from GitHub Actions!"

